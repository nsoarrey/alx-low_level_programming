
/**
 * function that prints the sign of aWrite a function that prints the sign of aWrite a function that print *the sign of a number. 
 */

int print_sign(int n)
{
  int n;
  if (n > 0)
    _putchar('+');
    return (1);
  else if (n < 0)
    _putchar('-');
    return (-1);
  else
    _putchar('0');  
    return (0);
}  
